# Next.js Client Environment Variables
# Copy this file to client/.env.local for local development
# For Docker, these are passed via docker-compose.yml or GitHub Actions secrets (production)

# Public API endpoint (used by client to call backend)
NEXT_PUBLIC_API_ENDPOINT=http://localhost:4000

# Public URL of the application (used for SEO, OG images, etc.)
NEXT_PUBLIC_URL=http://localhost:3000

